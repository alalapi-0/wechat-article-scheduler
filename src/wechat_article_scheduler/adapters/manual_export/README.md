# Manual Export

Manual export adapters generate outbox packages and require proof_of_publish before published.

Manual export is backlog in the current phase. It must not replace or complicate the active WeChat scan/plan/run-once flow.
