# Core

Future core interfaces for content packages, state machines, manifest loading and dry-run validation. Current WeChat CLI flow is not routed through this package yet.

These interfaces are backlog scaffolding. The current P0 implementation remains the WeChat Official Account workflow.
